services:
  mcts:
    build:
      context: .
      dockerfile: docker/Dockerfile
    working_dir: /workspace/MCTS
    volumes:
      - .:/workspace/MCTS
    environment:
      MCTS_STYLE_TOOLS_DIR: /opt/mcts-style-tools/bin
    command: sleep infinity
